{
  "name": "@marcus/integration-tests",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "dependencies": {
    "@marcus/cli": "workspace:*",
    "@marcus/contracts": "workspace:*",
    "@marcus/protocol-client": "workspace:*",
    "@marcus/service": "workspace:*",
    "@marcus/sdk": "workspace:*",
    "@marcus/storage-sqlite": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json",
    "test": "bun test ./src"
  }
}
